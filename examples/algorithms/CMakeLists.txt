
add_example(level_bfs)
add_example(parent_bfs)
add_example(breadth_first_search)
add_example(maximal_independent_set)
add_example(betweenness_centrality)
add_example(sssp)
add_example(triangle_counting)
